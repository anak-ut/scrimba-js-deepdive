// challenges 1
const isAuth = true;
let shopChart;

shopChart = isAuth? 'iPad' : '';
console.log(`challenges 1 --- shopchart : ${shopChart}`);
console.log(' ----------------------------------- ');

const umr = 20;
let bolehAkses;

bolehAkses = umr >= 17 ? true : false ;
console.log(`challenges 2 -- boleh akses: ${bolehAkses}`);